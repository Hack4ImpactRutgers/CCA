{
  "name": "cca-monorepo",
  "version": "1.0.0",
  "description": "Combined Community Action",
  "scripts": {
    "start": "concurrently \"cd frontend && pnpm run dev\" \"cd backend && npm run dev\"",
    "postinstall": "cd frontend && pnpm install && cd ../backend && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
